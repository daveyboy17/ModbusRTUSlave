[project]
name = "modbus-slave-simulator"
version = "0.1.0"
description = "Cross-platform Modbus RTU Slave Simulator for embedded firmware testing"
requires-python = ">=3.11"

dependencies = [
    "PySide6>=6.7",
    "pyserial>=3.5"
]

[tool.ruff]
line-length = 100

[tool.black]
line-length = 100
